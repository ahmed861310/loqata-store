# متجر لقطة — الإصدار 2.1.0

## الجديد
- حساب عميل محلي على نفس الجهاز لحفظ الاسم والهاتف والعنوان.
- تعبئة بيانات العميل تلقائيًا عند إتمام الطلب.
- عرض الطلبات السابقة المرتبطة برقم الهاتف.
- لا يزال المشروع يعمل بدون خادم أو قاعدة بيانات خارجية.

## التشغيل
افتح `index.html` في المتصفح.


## الإصدار 2.2.0
- إضافة شاشة دخول محلية للوحة التحكم برمز افتراضي: `1234`.
- تحسين فصل واجهة الإدارة عن واجهة العميل.
- تنبيه واضح بأن القفل محلي وليس حماية لخادم أو قاعدة بيانات.

## الإصدار 3.3.0 — قاعدة بيانات وتسجيل دخول فعلي (نسخة تمهيدية)

أضيف خادم Node.js بدون مكتبات خارجية، مع قاعدة بيانات JSON محلية داخل `data/db.json`، وتسجيل دخول بجلسات Cookie، وتشفير كلمات المرور باستخدام `scrypt`.

### التشغيل

1. ثبّت Node.js 18 أو أحدث.
2. افتح مجلد المشروع في الطرفية.
3. شغّل: `npm start`
4. افتح: `http://localhost:3000`

### إنشاء حساب المدير لأول مرة

أرسل طلب POST إلى `/api/admin/bootstrap` يحتوي على `name` و`email` و`password` (10 أحرف على الأقل). بعد إنشاء المدير يصبح المسار مغلقًا.

> تنبيه: هذه نسخة تأسيسية للتجربة المحلية. قبل النشر الفعلي يجب استخدام PostgreSQL/MySQL، وHTTPS، ومتغيرات بيئة، وحماية CSRF، ومحددات محاولات الدخول، ونظام صلاحيات ومراجعة أمنيّة.


## الإصدار 3.3.0
- تحميل المنتجات من قاعدة البيانات عند فتح المتجر.
- حفظ الطلبات في `data/db.json` عبر API بدل الاعتماد على التخزين المحلي فقط.
- خصم المخزون من الخادم عند إنشاء الطلب.
- إضافة مسارات الإدارة لتعديل وحذف المنتجات وتحديث حالة الطلب.
- تنبيه: قاعدة JSON مناسبة للتجربة المحلية، وليست بديلًا عن PostgreSQL/MySQL عند النشر التجاري.


## الإصدار 2.6.0
- ربط لوحة الإدارة بواجهات الخادم لإضافة وتعديل وحذف المنتجات.
- تحديث حالات الطلبات وحذفها من قاعدة البيانات.
- أصبحت عمليات الإدارة تحفظ في `data/db.json` بدلًا من `localStorage`.
- يجب تسجيل دخول المدير عبر حساب المدير الذي يتم إنشاؤه باستخدام `/api/admin/bootstrap`.


## الإصدار 2.7.0 — الصلاحيات وحماية الجلسات

- إضافة مدة صلاحية للجلسة: 7 أيام.
- التحقق من صلاحية الجلسة قبل تنفيذ الطلبات.
- حصر عمليات الإدارة على حسابات `admin` فقط.
- التحقق من حالات الطلبات المسموح بها.
- إضافة نقطة API لقراءة بيانات المستخدم الحالي: `/api/profile`.
- تنبيه: هذه نسخة تطويرية؛ عند النشر يجب استخدام HTTPS وقاعدة بيانات إنتاجية ومخزن جلسات دائم.


## الإصدار 2.8.0
- صفحة حساب العميل مرتبطة بـ `/api/profile` و`/api/orders/mine`.
- عرض الطلبات الخاصة بالحساب المسجل فقط.
- حفظ الهاتف والعنوان محليًا لتسهيل إتمام الطلبات.
- إغلاق نافذة الحساب من زر الإغلاق أو الخلفية.

## الإصدار 3.0.0
- إضافة إشعارات حالة الطلبات للعملاء.
- حفظ سجل تغييرات حالة الطلب.
- API جديد: `/api/notifications`.

## الإصدار 3.0.0 — كوبونات الخصم
- إضافة كود خصم اختياري أثناء الطلب.
- التحقق من الكوبون عبر الخادم.
- دعم خصم بنسبة مئوية أو مبلغ ثابت.
- دعم حد أدنى لقيمة الطلب وتاريخ انتهاء.
- واجهات إدارية لإنشاء وعرض وحذف الكوبونات.
- تنبيه: هذه نسخة تطويرية؛ راجع سياسات الاستخدام ومعدلات الطلب قبل النشر العام.


## v3.2.0 — عروض المنتجات
- يدعم المنتج `salePrice` كسعر عرض اختياري أقل من السعر الأصلي.
- يدعم `offerEndsAt` لتحديد موعد انتهاء العرض.
- يعرض السعر القديم والجديد ونسبة الخصم تلقائيًا للعميل.
- يتم احتساب سعر العرض فعليًا عند إنشاء الطلب من الخادم، وليس من واجهة المتصفح.
- يمكن للمدير تعديل سعر العرض وموعد انتهائه من بطاقة المنتج داخل لوحة التحكم.
- ترك سعر العرض فارغًا أو وضع موعد منتهي يعيد المنتج لسعره الأصلي.


## v3.3.0 — الشحن والتوصيل
- مناطق توصيل ورسوم شحن قابلة للتعديل من لوحة الإدارة.
- شحن مجاني فوق حد يحدده المدير.
- حساب الشحن والخصم والإجمالي النهائي على الخادم.
- حفظ منطقة الشحن وتفاصيل المبلغ داخل الطلب.
