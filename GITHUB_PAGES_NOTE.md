# نشر متجر لقطة

هذه النسخة تعتمد على `server.js` لتشغيل API وتسجيل الدخول وOTP والطلبات ولوحة الإدارة والدفع.

## مهم
GitHub Pages يستضيف الملفات الثابتة فقط، لذلك لن يعمل `/api/*` عليه. ظهور رسالة "الخادم غير متصل" عند فتح الموقع من `github.io` يعني أن الواجهة تعمل لكن خادم Node غير منشور.

## النشر الصحيح
انشر المشروع كاملًا على خدمة تستضيف Node.js، ثم افتح رابط الخادم نفسه. يجب أن يعمل:

- `GET /api/health`
- `/api/auth/*`
- `/api/admin/*`
- `/api/orders`
- `/api/payments/*`

بعد تشغيل الخادم يمكن تجهيز Paymob وربط OTP الفعلي.
