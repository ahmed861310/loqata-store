# اختبار Paymob قبل الإطلاق

هذه النسخة تضيف اختبارًا آليًا محليًا لمسار Paymob بدون إرسال أي عملية مالية حقيقية.

## الاختبار المحلي

```bash
npm run test:paymob
```

الاختبار يتحقق من:
- إنشاء Payment Intention عبر بوابة Paymob وهمية.
- Webhook ناجح يغيّر الدفع إلى `paid` والطلب إلى `قيد التجهيز`.
- رفض Webhook بتوقيع غير صحيح.
- Webhook فاشل يغيّر الدفع إلى `failed` والطلب إلى `ملغي`.
- Endpoint جاهزية الإنتاج مع جميع المتغيرات المطلوبة.

## الاختبار الحقيقي

قبل الإطلاق الفعلي يجب تشغيل Paymob TEST Credentials على بيئة عامة HTTPS، ثم تنفيذ:
1. عملية دفع ناجحة.
2. عملية فاشلة/ملغاة.
3. التأكد من وصول Webhook.
4. التأكد من ظهور الحالة الصحيحة في لوحة التحكم.

الاختبار المحلي لا يُعتبر بديلًا عن الاختبار الحقيقي على حساب Paymob.
