# متجر لقطة v5.18.1 — إصلاح إنشاء حساب المدير

- إضافة فحص واضح لحالة إنشاء المدير قبل عرض زر الإنشاء.
- إذا كان المدير موجودًا، يظهر للمستخدم أنه يجب تسجيل الدخول بدل ظهور خطأ عام.
- تحسين تحقق البريد الإلكتروني وكلمة المرور في الخادم.
- دعم قاعدة بيانات قديمة لا تحتوي على `users` كمصفوفة دون التسبب في خطأ خادم.
- إضافة كود خطأ واضح `ADMIN_EXISTS` عند محاولة إنشاء مدير ثانٍ.
- لا توجد أي بيانات Paymob Live أو أسرار حقيقية داخل النسخة.

## الاختبارات
- `node --check server.js` — PASS
- `node --check app.js` — PASS
- `npm run test:e2e` — 19/19 PASS
